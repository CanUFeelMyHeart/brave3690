1391499931841894891491494119491409491949194
4419418914881481484814848841818488481481481481848148481488141481848189418481948198491981518г581985915814545
Привет, Гитхаб! Здесь будет информация о первых командах из 3 семинара
> git push - отправить изменения в удаленный репозиторий 
Привет мы пробуем работу с удаленным репозиторием =)

## Работа с удаленными репозиториями в git

**Как сделать pull request**
* Делаем fork репозитория
* Делаем clone СВОЕЙ версии репозитория
* Создаем новую ветку и в НЕЕ вносим свои изменения
* Фиксируем изменения (делаем коммиты)
* Отправляем свою версию в свой GitHub
* На сайте GitHub нажимаем кнопку pull request

Для ввода имени используем: git config --global user.name “Your_name”
Для ввода почты используем: git config --global user.email ваша почта@example.com
Вставляем свое имя и почту, к примеру:
git config --global user.name “Mikhail”
git config --global user.email msmerkushov123@yandex.ru

git --version
git init - инициализировать в гит
git status - что думает гит об этой проге
git add добавить (отслеживать)

[ctrl + s] - сохронение

git commit -m "Сообщение  комент"  -   зафиксировать
git log - чтобы видеть какие верссии сущ 
git checkout 47ah (указать какое сохр надо загру- цифры целиком или 4 символа)
git checkout master (main) тобы знать на какой мы ветви - на основной Вернуться на актуальную версию

git diff - показывает разницу

git add helli (то есть добавляем тот файл, который хотим сохронить)
Затем
git commit -m "Сообщение  комент" - cохроняем
кнопка Q чтобы выйти из режима END

dir Листининг текущей директории
*Курсив.* (звездочки для написания курсива)
**Полужирный шрифт**
*+пробел Список 
1.+пробел   Нумерованные списки
# - тема название заголовка
## - выделение текста

Для языка .md Марк даун надо отступать 1 страку, т.к. иначе текст будит слитный

# Инструкция Марк даун и Git
(Заголовок первого уровня)
## Заголовок второго уровня

*Курсив*

**Полужирный шрифт**

***Полужирный с курсивом***

~~Зачеркнутый текст~~

* ненумерованные списки

1. Нумерованные страницы

> Первый уровень цитирования
>> Второй уровень цитирования

### Web ссылки 
текст [Пример ссылки](http.example.com "всплывающая подсказка")


clear (Очистить терминал)

git branch new_text (создаем новую ветку, черновую)
- затем переходим на эту ветку: git checkout new_text
- git log
- it checkout master (main) - перейти на главн ветку

Cоздадим еще ветку для этого надо вернуться на основную ветку, и от тудого создать ветку еще раз

Потом чтобы из черновика добавить в главную ветку main, надо из черновика вернуться на главную ветку и от тудого с пом git merge new_text (название ветки)
git merge new_text (название ветки) - чтобы слить ее с главной веткой

git branch -d (и название ветки new_text) - удаление ненужной уже ветки. 
git branch -D new_text - удаление ненужной уже ветки. Грубое, оно не сохранилось

*Kursiv*
**Polujirniy**
_Kursiv_
__Polujirniy__
_Ves tekst kursiv, no eto slovo **polujirniy** kursiv_

![]() для картинки
![робокоп](Robocop.jpg) для картинки

.gitignore - Сщздаем пвпку для игнорирования картинок, затем пишем туда файлы c расширением. Затем сохор, git add .gitignore  zатем  git commit -m ""

git log --graph  - 

git log --oneline --graph - показать все коммиты и ветви в графике


Удаленная работа
git clone (и ссылка репозитория в гитхабе) - то чтобы репозиторий др из гит хаб появился у тебя на компе

cd (название папки) - чтобы гит инициализировал др папку с репозиторием

Связать локальный репозиторий с удаленным репозиторием
…or create a new repository on the command line
echo "# Murad1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/MuradShamatov/Murad1.git
git push -u origin main


…or push an existing repository from the command line
git remote add origin https://github.com/MuradShamatov/Murad1.git
git branch -M main
git push -u origin main - выталкать все изменения в гитхаб (орижин)


git push - отправить свой репозиторий себе в github

git pull - Стянуть из github свой дополненный репозиторий 

Fork (кнопка в github) - для скачки чужего репозитория к себе в аккаунт в гитхаб
А потом уже с нашего аккаунта можим взять ссылку
git clone (и ссылка репозитория в гитхабе) - то чтобы репозиторий др из гит хаб появился у тебя на компе
cd (название папки) - чтобы гит инициализировал др папку с репозиторием
создаем чкрновую ветку - git branch (и название) 
потом переходим к ней - git checout (и название ветки)
Сщздаем новый файл в этой папке
после изменений, комитем  и  мы должны предложить др чел, откудого брали Fork
git push --set-upstream origin ((и название файла) disk1) -отправляем или пишем git push и оно сомо выйдет



git push --set-upstream origin (и название новой ветки) - если создали новую ветку, то ее надо так писать для того чтобы выталкать изменения в гитхаб

git branch -M main - переименовать текущую ветку на main (вместо master)


git remote show (он покажит связь с origin (это акк с github))

git remote -v

git remote show origin



git pull --rebase - если и у тебя изменения и в гитхаб изменения одновременно. Чтобы слияние было. Значит будит выкачка из гитхвб, а после согласимся со сдиянием и отправил ее в гитхаб

 


## GIT

Представиться гиту - имя и имаил
```sh
git config --global user.name "Murad"
```
```sh
git config --email "smart1992m@gmail.com"
```

Команда смены директории для мак и линукс

```sh
cd c:/folder_name
```

команда отображение текущей директории в Мак и Линукс. В виндовс это не требовалось
```sh
pwd
```
Листининг текущей директории в виндовс
```sh
dir
```
Листининг текущей директории в в мак и линукс
```sh
rm
```

В мак и линукс
```sh
ls
```
Версия гита
```sh
git --version
```
Инициализация этой папки
```sh
git init
```
Cтатус папки. содержимое, разница изменений
```sh
git status
```
вывод на экран истории всех коммитов с их хеш-кодами
```
git log
```
переход от одного коммита к другому
```
git checkout (и указать номер коммита 4 символа)
```
Вернуться к актуальному состоянию
``````
git checkout master (main)
``````
Удалить файл в папке Мак и линукс
```sh
rm
```
Удалить в папке Виндовс
``````
dell и указываем путь
``````
Увидеть разницу между текущим файлом и закоммиченным файлом
``````
git diff
``````
В укороченном виде отоброжение коммитов
```
git log --oneline
```
Переход на последнию версию коммита
```sh
git checkout master (или main)
````
Возврат изменений до зафиксирования коммита
``````
Git restore (+ название папки)